```
conda create --prefix /path/to_folder/langchain_tutorial python=3.12.2
conda activate langchain_tutorial
pip install -r requirements.txt
python3 -m ipykernel install --user --name langchain_tutorial
```